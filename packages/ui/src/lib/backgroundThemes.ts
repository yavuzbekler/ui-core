// Re-export background themes from @yavuzbekler/tokens (single source of truth)
export {
  BACKGROUND_THEMES,
  BACKGROUND_THEME_NAMES,
  DEFAULT_BACKGROUND_THEME,
} from '@yavuzbekler/tokens';

export type { BackgroundThemeName, BackgroundThemePalette, BackgroundTheme } from '@yavuzbekler/tokens';
