// Re-export background themes from @yvz-ui-core/tokens (single source of truth)
export {
  BACKGROUND_THEMES,
  BACKGROUND_THEME_NAMES,
  DEFAULT_BACKGROUND_THEME,
} from '@yvz-ui-core/tokens';

export type { BackgroundThemeName, BackgroundThemePalette, BackgroundTheme } from '@yvz-ui-core/tokens';
